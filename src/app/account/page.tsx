import Link from "next/link";
import { requireAccount } from "@/data/auth";
export default async function AccountPage(){const account=await requireAccount();return <main className="min-h-screen bg-[#eee6da] p-5 sm:p-10"><section className="mx-auto max-w-2xl rounded-[2rem] border border-line bg-paper p-7 soft-shadow sm:p-10"><p className="eyebrow text-wine">Account</p><h1 className="mt-2 font-display text-4xl">Profile & security</h1><dl className="mt-8 grid gap-5 rounded-2xl bg-background p-5 sm:grid-cols-2"><div><dt className="text-xs font-bold uppercase tracking-wider text-muted">Name</dt><dd className="mt-1 font-semibold">{account.displayName}</dd></div><div><dt className="text-xs font-bold uppercase tracking-wider text-muted">Role</dt><dd className="mt-1 capitalize">{account.role}</dd></div><div className="sm:col-span-2"><dt className="text-xs font-bold uppercase tracking-wider text-muted">Email</dt><dd className="mt-1">{account.user.email}</dd></div></dl><div className="mt-7 flex flex-wrap gap-3"><Link href={`/${account.role}`} className="rounded-full bg-wine px-5 py-3 text-sm font-semibold text-white">Return to workspace</Link><Link href="/forgot-password" className="rounded-full border border-line px-5 py-3 text-sm font-semibold">Reset password</Link>{account.role==="admin"&&<Link href="/mfa" className="rounded-full border border-line px-5 py-3 text-sm font-semibold">Authenticator security</Link>}</div></section></main>}
