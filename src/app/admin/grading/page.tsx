import { DashboardHeading, Panel, StatusPill } from "@/components/dashboard-ui";
import { gradeRules } from "@/lib/grading";
export default function GradingPage(){return <div className="mx-auto max-w-7xl"><DashboardHeading eyebrow="Configurable scorecard" title="Grade policy" description="Changes apply at the next nightly recalculation and are written to policy history."/><Panel className="overflow-x-auto"><table className="w-full min-w-[760px] text-left text-sm"><thead className="border-b border-line bg-background text-[10px] uppercase tracking-wider text-muted"><tr><th className="p-4">Grade</th><th>Jobs</th><th>Rating</th><th>On time</th><th>Cancellation max</th><th>Lost disputes max</th><th>Capacity</th></tr></thead><tbody><tr className="border-b border-line"><td className="p-4"><StatusPill tone="gray">Grade 1</StatusPill></td><td>0</td><td>—</td><td>—</td><td>—</td><td>—</td><td className="font-semibold">2</td></tr>{[...gradeRules].reverse().map(rule=><tr key={rule.grade} className="border-b border-line last:border-0"><td className="p-4"><StatusPill>Grade {rule.grade}</StatusPill></td><td>{rule.minJobs}</td><td>{rule.minRating}</td><td>{rule.minOnTime}%</td><td>{rule.maxCancellation}%</td><td>{rule.maxLostDisputes}%</td><td className="font-semibold">{rule.capacity}</td></tr>)}</tbody></table></Panel><div className="mt-4 rounded-xl border border-line bg-[#eee6da] p-4 text-xs leading-5 text-muted">A demotion never cancels existing jobs. New quote acceptance is blocked until active jobs and temporary reservations fall below the new capacity.</div></div>}
