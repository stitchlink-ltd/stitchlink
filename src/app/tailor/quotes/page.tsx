import { Clock3, MessageCircle } from "lucide-react";
import { DashboardHeading, Panel, StatusPill } from "@/components/dashboard-ui";
import { Price } from "@/components/price";
export default function TailorQuotesPage(){return <div className="mx-auto max-w-6xl"><DashboardHeading eyebrow="Requests and negotiation" title="Quotes" description="Only the latest structured revision can be accepted by a customer."/><div className="grid gap-4">{[["Modern ivory agbada","Chidi E.",28000000,"19 hours"],["Wedding guest two-piece","Maya K.",16500000,"2 days"],["Linen destination suit","Andre P.",31000000,"3 days"]].map(([title,customer,amount,expiry],index)=><Panel key={title as string} className="grid gap-4 p-5 md:grid-cols-[1fr_auto_auto] md:items-center"><div className="flex gap-3"><span className="grid size-10 place-items-center rounded-full bg-wine/10 text-wine"><MessageCircle size={17}/></span><div><div className="flex items-center gap-2"><h2 className="font-semibold">{title as string}</h2><StatusPill tone={index===0?"wine":"gold"}>{index===0?"Reply needed":"Draft"}</StatusPill></div><p className="mt-1 text-xs text-muted">{customer as string} · international customer</p></div></div><div><p className="text-[10px] uppercase tracking-wider text-muted">Proposed budget</p><Price kobo={amount as number} className="font-semibold"/></div><button className="rounded-full bg-wine px-4 py-2 text-xs font-semibold text-white"><Clock3 size={12} className="mr-1 inline"/>Expires in {expiry as string}</button></Panel>)}</div></div>}
