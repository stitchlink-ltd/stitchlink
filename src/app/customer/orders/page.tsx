import Link from "next/link";
import { ArrowRight, Scissors } from "lucide-react";
import { DashboardHeading, Panel, StatusPill } from "@/components/dashboard-ui";
import { Price } from "@/components/price";
import { requireRole } from "@/data/auth";
import { getCustomerOrders } from "@/data/marketplace";
export default async function OrdersPage(){const account=await requireRole("customer");const orders="demo" in account?[]:await getCustomerOrders(account.user.id);return <div className="mx-auto max-w-6xl"><DashboardHeading eyebrow="Order history" title="My orders" description="Active commissions and completed pieces."/>{orders.length?<Panel className="divide-y divide-line overflow-hidden">{orders.map(order=><Link key={order.id} href={`/customer/orders/${order.id}`} className="flex flex-col gap-4 p-5 hover:bg-background sm:flex-row sm:items-center"><span className="grid size-12 place-items-center rounded-xl bg-background text-wine"><Scissors size={20}/></span><div className="flex-1"><div className="flex items-center gap-2"><h2 className="font-semibold">{order.title}</h2><StatusPill>{order.stage}</StatusPill></div><p className="mt-1 text-xs text-muted">{order.reference} · Due {order.dueDate}</p></div><Price kobo={order.totalKobo} className="font-semibold"/><ArrowRight size={16}/></Link>)}</Panel>:<Panel className="p-8 text-sm text-muted">No orders yet. Submit a request to begin.</Panel>}</div>}
